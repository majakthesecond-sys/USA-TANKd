# USA Tank Battle — Online 1v1 (Render)

## Local run
```bash
npm install
npm start
```
Open: http://localhost:3000/

## Deploy on Render
Create a **Web Service** from this repo. Set:
- Build Command: `npm install`
- Start Command: `npm start`

Then open your Render URL and choose **Online 1v1 (WebSocket)** in the mode list.
